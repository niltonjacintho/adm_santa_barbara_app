import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('PastoraiseditPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PastoraiseditPage(title: 'Pastoraisedit')));
    //  final titleFinder = find.text('Pastoraisedit');
    //  expect(titleFinder, findsOneWidget);
  });
}
