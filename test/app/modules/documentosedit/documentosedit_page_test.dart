import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('DocumentoseditPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DocumentoseditPage(title: 'Documentosedit')));
    //  final titleFinder = find.text('Documentosedit');
    //  expect(titleFinder, findsOneWidget);
  });
}
