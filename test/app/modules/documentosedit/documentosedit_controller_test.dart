import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:admFlutter/app/modules/documentosedit/documentosedit_module.dart';

void main() {
  initModule(DocumentoseditModule());
  // DocumentoseditController documentosedit;
  //
  setUp(() {
    //     documentosedit = DocumentoseditModule.to.get<DocumentoseditController>();
  });

  group('DocumentoseditController Test', () {
    //   test("First Test", () {
    //     expect(documentosedit, isInstanceOf<DocumentoseditController>());
    //   });

    //   test("Set Value", () {
    //     expect(documentosedit.value, equals(0));
    //     documentosedit.increment();
    //     expect(documentosedit.value, equals(1));
    //   });
  });
}
