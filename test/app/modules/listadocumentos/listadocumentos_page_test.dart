import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('ListadocumentosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ListadocumentosPage(title: 'Listadocumentos')));
    //  final titleFinder = find.text('Listadocumentos');
    //  expect(titleFinder, findsOneWidget);
  });
}
