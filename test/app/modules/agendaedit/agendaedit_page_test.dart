import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('AgendaeditPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AgendaeditPage(title: 'Agendaedit')));
    //  final titleFinder = find.text('Agendaedit');
    //  expect(titleFinder, findsOneWidget);
  });
}
