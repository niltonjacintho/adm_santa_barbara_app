import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('DocumentoeditPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DocumentoeditPage(title: 'Documentoedit')));
    //  final titleFinder = find.text('Documentoedit');
    //  expect(titleFinder, findsOneWidget);
  });
}
